<h1>Super Hero: <%= @heroine.super_name %></h1>

<p>Name: <%= @heroine.name %></p>
<p>Powers:</p>
<ul>
    <% @heroine.powers.each do |power| %>
        <li><%= link_to power.name, power_path(power) %>
    <% end %>
</ul>