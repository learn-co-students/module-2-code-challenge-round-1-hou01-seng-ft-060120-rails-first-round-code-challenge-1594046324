<%= form_for @heroine do |f| %>
    # <% if @heroine.errors.any? %>
    # <% @heroine.errors.full_messages.each do |message| %>
    # <li><%= message %></li>
    # <% end %>
    # <% end %>  ---- tried my best, but i've NEVER been able to display validation error msgs :(
    <%= f.label :name %>
    <%= f.text_field :name %>
    <br>
    <br>
    <%= f.label :super_name %>
    <%= f.text_field :super_name %>
    <br>
    <br>
    <%= f.collection_select(:power_ids, @powers, :id, :name, prompt:"Choose a Super Power") %>
    <br>
    <br>
    <%= f.submit %>
    <% end %>
   